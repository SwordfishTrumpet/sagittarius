# Sagittarius Environment Configuration
# Copy this file to .env and adjust values as needed.

# JMAP server URL (used by Vite dev server proxy)
# Default: http://localhost:8080
VITE_JMAP_SERVER=http://localhost:8080

# Optional login alias domain. When set, Sagittarius accepts bare usernames
# like "user" and also retries "user@example.com" plus
# internal usernames such as "user-example" when needed.
# VITE_LOGIN_EMAIL_DOMAIN=example.com

# RFC 9749 WebPush VAPID public key (required for browser push notifications)
# Generate with: npx web-push generate-vapid-keys
# VITE_VAPID_PUBLIC_KEY=
